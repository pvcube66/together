/** Matches textured panels used across dashboard / rooms / profile. */
export const PANEL_TEXTURE =
  "bg-[color:var(--panel-texture-bg)] bg-[image:var(--panel-texture-image)] bg-[length:200px_200px] rounded-2xl border border-border/50 shadow-[var(--panel-shadow-sm)]";
